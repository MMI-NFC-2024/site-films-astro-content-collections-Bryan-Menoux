---
import type { CollectionEntry } from 'astro:content'

interface Props {
    personne: CollectionEntry<"personnes">
}
const { personne } = Astro.props;
---

<a href={`/personnes/${personne.id}`} class="hover:underline">{personne.data.nom}</a>